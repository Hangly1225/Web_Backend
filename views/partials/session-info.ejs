<div>
  <% if (user) { %>
    Logged in as <strong><%= user %></strong>
    <% if (role) { %>
      (<%= role %>)
    <% } %>
    
    | <a href="/logout">Logout</a>
    <% } else { %>
      <a href="/login">Login</a> | <a href="/signin">Signin</a>
    <% } %>
  </div>